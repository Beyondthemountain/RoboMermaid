{
  "name": "diagram-build-tools",
  "private": true,
  "version": "1.0.0",
  "description": "Tooling to generate and render architecture diagrams from YAML",
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.0.0"
  },
  "scripts": {
    "build:diagrams": "bash scripts/build_diagrams.sh"
  }
}
